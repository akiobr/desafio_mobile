name: desafio_mobile
description: Projeto desafio mobile bycoders_

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.1.0
  equatable: ^2.0.3   
  google_fonts: ^2.1.0
  formz: ^0.4.0
  google_maps_flutter: ^2.0.6
  location: ^4.3.0
  firebase_core: ^1.4.0
  firebase_auth: ^3.0.1
  firebase_analytics: ^8.2.0
  firebase_crashlytics: ^2.1.1
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  bloc_test: ^8.0.2  
  mocktail: ^0.1.0
  authentication_repository:
    path: packages/authentication_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.1
  hive_generator: ^1.1.0

flutter:
  uses-material-design: true