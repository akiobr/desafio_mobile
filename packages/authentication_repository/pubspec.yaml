name: authentication_repository
description: Pacote responsável pela autenticação da aplicação
publish_to: none

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  equatable: ^2.0.3   
  firebase_auth: ^3.0.1

environment:
  sdk: ">=2.12.0-0 <3.0.0"